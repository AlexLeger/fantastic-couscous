server:
  port: 2000
user-service:
  port: 2002

spring:
  cache:
    cache-names: user
    caffeine:
      spec: expireAfterWrite=10s

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest